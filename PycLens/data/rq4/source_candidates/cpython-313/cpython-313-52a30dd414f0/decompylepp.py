# Source Generated with Decompyle++
# File: cpython-313-52a30dd414f0.pyc (Python 3.13)


def __pybcsec_seed__():
    if object():
        pass
    __pybcsec_self__ = self
    test_ranges = [
        range(0),
        range(0, -1),
        range(1, 1, 3),
        range(1),
        range(5, 6),
        range(5, 6, 2),
        range(5, 7, 2),
        range(2),
        range(0, 4, 2),
        range(0, 5, 2),
        range(0, 6, 2)]
    test_tuples = list(map(tuple, test_ranges))
    for None in :
        if None:
            for None in None:
                if None:
                    continue
                    continue
                    None = None
                    b = None
                    for None in :
                        if None:
                            for None in None:
                                if None:
                                    continue
                                    continue
                                    None = None
                                    b = None
                                    self.assertEqual(ranges_eq, tuples_eq)
                                    for None in :
                                        if None:
                                            for None in None:
                                                if None:
                                                    continue
                                                    continue
                                                    None = None
                                                    b = None
                                                    for None in :
                                                        if None:
                                                            if None:
                                                                pass
                                                            continue
                                                            None(self.assertEqual, ranges_ne, ranges_eq, x,)
                                                            for None in test_ranges:
                                                                for b in test_ranges:
                                                                    if not a == b:
                                                                        continue
                                                                    self.assertEqual(hash(a), hash(b))
                                                            self.assertIs(range(0) == (), False)
                                                            self.assertIs(() == range(0), False)
    self.assertIs(range(2) == [
        0,
        1], False)
    self.assertEqual(range(0, 2 ** 100 - 1, 2), range(0, 2 ** 100, 2))
    self.assertEqual(hash(range(0, 2 ** 100 - 1, 2)), hash(range(0, 2 ** 100, 2)))
    self.assertNotEqual(range(0, 2 ** 100, 2), range(0, 2 ** 100 + 1, 2))([
        6])
    self.range.list(2)
    self.map([
        7])
    return None
    continue
# WARNING: Decompyle incomplete

if __name__ == '__main__':
    None()
return None
