[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "PycLens"
version = "0.1.0"
description = "PycLens empirical study tool for Python bytecode artifacts."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
  {name = "PycLens Authors"}
]
dependencies = []

[project.scripts]
pyclens = "cli:main"

[tool.setuptools]
py-modules = [
  "analysis",
  "cli",
  "collectors",
  "scanner",
  "tool_analysis",
  "cpython_fuzz",
  "seed_extract",
  "source_repro",
  "crash_analysis",
  "rq1_analysis",
]
package-dir = {"" = "src"}
